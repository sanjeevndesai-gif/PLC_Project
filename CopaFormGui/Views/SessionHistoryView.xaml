<UserControl x:Class="CopaFormGui.Views.SessionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="6" Padding="16,10" Margin="0,0,0,8">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Session History" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#BBDEFB" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Active Session Banner -->
        <Border Grid.Row="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,8">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#E8F5E9"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasActiveSession}" Value="False">
                            <Setter Property="Background" Value="#ECEFF1"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <Ellipse Width="12" Height="12" Margin="0,0,8,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#4CAF50"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasActiveSession}" Value="False">
                                    <Setter Property="Fill" Value="#9E9E9E"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock Text="{Binding ActiveSessionInfo}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#2E7D32"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasActiveSession}" Value="False">
                                    <Setter Property="Foreground" Value="#546E7A"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- Session History Table -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="8">
            <DataGrid ItemsSource="{Binding Sessions}"
                      SelectedItem="{Binding SelectedSession}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Operator"    Binding="{Binding OperatorName}"                                   Width="120"/>
                    <DataGridTextColumn Header="Start Time"  Binding="{Binding StartTime, StringFormat={}{0:dd/MM/yy HH:mm:ss}}" Width="140"/>
                    <DataGridTextColumn Header="End Time"    Binding="{Binding EndTime, StringFormat={}{0:dd/MM/yy HH:mm:ss}}"   Width="140"/>
                    <DataGridTextColumn Header="Duration"    Binding="{Binding DurationText}"                                   Width="120"/>
                    <DataGridTextColumn Header="Punches"     Binding="{Binding TotalPunches}"                                   Width="70"/>
                    <DataGridTextColumn Header="Programs Run" Binding="{Binding ProgramsRunText}"                               Width="*"/>
                </DataGrid.Columns>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter Property="Background" Value="#E8F5E9"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>

        <!-- Action Buttons -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="8,6" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Refresh"
                        Command="{Binding RefreshCommand}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Margin="0,0,8,0"/>
                <Button Content="Clear History"
                        Command="{Binding ClearHistoryCommand}"
                        Style="{StaticResource DangerButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
