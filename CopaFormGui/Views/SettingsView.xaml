<UserControl x:Class="CopaFormGui.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="6" Padding="16,10" Margin="0,0,0,8">
            <Grid>
                <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Foreground="White"/>
            </Grid>
        </Border>

        <!-- Tabs -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <!-- Tab 1: General -->
            <TabItem Header="General">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="8,0,8,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Times Section -->
                        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,8,8,0">
                            <StackPanel>
                                <TextBlock Text="Times" Style="{StaticResource SectionHeaderStyle}"/>
                                <StackPanel>
                                    <TextBlock Text="Supervise-time punching" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SuperviseTimePunching}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Running time belt with workpiece" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding RunningTimeBeltWorkpiece}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Running time belt with rest" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding RunningTimeBeltRest}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Waiting time closing grippers" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding WaitingTimeClosingGrippers}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Waiting time open grippers" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding WaitingTimeOpenGrippers}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Waiting time closing clamping" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding WaitingTimeClosingClamping}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Waiting time open clamping" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding WaitingTimeOpenClamping}" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Speed Settings Section -->
                        <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Speed Settings" Style="{StaticResource SectionHeaderStyle}"/>
                                <StackPanel>
                                    <TextBlock Text="Speed X" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SpeedX}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Speed Y" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SpeedY}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Speed Z" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SpeedZ}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Speed X (Hand)" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SpeedXHand}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Speed Y (Hand)" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SpeedYHand}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Speed Z (Hand)" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding SpeedZHand}" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Positions and lengths Section -->
                        <Border Grid.Row="1" Grid.ColumnSpan="2" Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Positions and lengths" Style="{StaticResource SectionHeaderStyle}"/>
                                <StackPanel>
                                    <TextBlock Text="Part drop off position" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding PartDropOffPosition}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Grab position gripper" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding GrabPositionGripper}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Changeover position punching" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding ChangeoverPositionPunching}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Changeover position cutting" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding ChangeoverPositionCutting}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Offset side stop" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding OffsetSideStop}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Zero point tool 4" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding ZeroPointTool4}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Change position tool 4" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding ChangePositionTool4}" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 2: Service -->
            <TabItem Header="Service">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
                    <StackPanel>
                        <!-- Axis Sections Row -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                            <!-- X Axis Section -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,8,8,0" Width="260">
                                <StackPanel>
                                    <TextBlock Text="X Axis (Transport direction)" Style="{StaticResource SectionHeaderStyle}"/>
                                    <TextBlock Text="Acceleration" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding XAxisAcceleration}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Unblocking position for Y-Axis" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding XAxisUnblockY}" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </Border>
                            <!-- Y Axis Section -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,8,8,0" Width="260">
                                <StackPanel>
                                    <TextBlock Text="Y Axis (Tool bar)" Style="{StaticResource SectionHeaderStyle}"/>
                                    <TextBlock Text="Acceleration" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding YAxisAcceleration}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Unblocking position for X-Axis right" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding YAxisUnblockXRight}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Unblocking position for X-Axis left" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding YAxisUnblockXLeft}" Style="{StaticResource InputTextBoxStyle}"/>
                                    <TextBlock Text="Side stop position" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding YAxisSideStop}" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </Border>
                            <!-- Z Axis Section -->
                            <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0" Width="260">
                                <StackPanel>
                                    <TextBlock Text="Z Axis (Punchcylinder)" Style="{StaticResource SectionHeaderStyle}"/>
                                    <TextBlock Text="Acceleration" Style="{StaticResource FormLabelStyle}"/>
                                    <TextBox Text="{Binding ZAxisAcceleration}" Style="{StaticResource InputTextBoxStyle}"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- Software Limit Switches Grid -->
                        <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="2*"/>
+                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Software limit switches" Style="{StaticResource SectionHeaderStyle}" Grid.ColumnSpan="3"/>
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="End position X-Minimum" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding XMin}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBlock Grid.Row="1" Grid.Column="2" Text="End position X-Maximum" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding XMax}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="End position Y-Minimum" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding YMin}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBlock Grid.Row="2" Grid.Column="2" Text="End position Y-Maximum" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding YMax}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="End position Z-Minimum" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ZMin}" Style="{StaticResource InputTextBoxStyle}"/>
                                <TextBlock Grid.Row="3" Grid.Column="2" Text="End position Z-Maximum" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding ZMax}" Style="{StaticResource InputTextBoxStyle}"/>
                            </Grid>
                        </Border>

                        <!-- Zero point and Balancer -->
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Content="Zero point" Width="120" Margin="0,0,8,0"/>
                            <Button Content="Balancer" Width="120"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 3: Tool Lengths -->
            <TabItem Header="Tool">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="4">
                    <StackPanel>
                        <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Tool Lengths (mm)" Style="{StaticResource SectionHeaderStyle}"/>
                                <TextBlock Text="Tool Length 1" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding ToolLength1}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10" Width="200" HorizontalAlignment="Left"/>
                                <TextBlock Text="Tool Length 2" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding ToolLength2}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10" Width="200" HorizontalAlignment="Left"/>
                                <TextBlock Text="Tool Length 3" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding ToolLength3}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10" Width="200" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource CardStyle}" Margin="0,8,0,0">
                            <StackPanel>
                                <TextBlock Text="Safety Settings" Style="{StaticResource SectionHeaderStyle}"/>
                                <TextBlock Text="Safety Height (mm)" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding SafetyHeight}" Style="{StaticResource InputTextBoxStyle}" Margin="0,0,0,10" Width="200" HorizontalAlignment="Left"/>
                                <TextBlock Text="Clamp Force (%)" Style="{StaticResource FormLabelStyle}"/>
                                <TextBox Text="{Binding ClampForce}" Style="{StaticResource InputTextBoxStyle}" Width="200" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Bottom Actions -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="8,6" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Save Settings"    Command="{Binding SaveSettingsCommand}"
                        Style="{StaticResource SuccessButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="Apply to Controller" Command="{Binding ApplySettingsCommand}"
                        Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="Reset Defaults"   Command="{Binding ResetDefaultsCommand}"
                        Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,16,0"/>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
