<UserControl x:Class="CopaFormGui.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="6" Padding="16,10" Margin="0,0,0,8">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Database" FontSize="18" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="DataBase Option Clicked – Below Table Should be Displayed"
                               FontSize="12" Foreground="#BBDEFB" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Notes Banner -->
        <Border Grid.Row="1" Background="#E3F2FD" CornerRadius="6" Padding="12,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="&#x1F4CB; " FontSize="14"/>
                <StackPanel>
                    <TextBlock Text="{Binding Notes}" FontSize="12" FontWeight="SemiBold" Foreground="#1565C0"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Data Table -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="8">
            <DataGrid ItemsSource="{Binding ProgramRecords}"
                      SelectedItem="{Binding SelectedRecord}"
                      PreviewMouseDoubleClick="RecordsDataGrid_MouseDoubleClick"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"             Binding="{Binding ProgramId}" Width="50" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Name"           Binding="{Binding ProgramName}" Width="140" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Material"       Binding="{Binding Material}" Width="90" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Comment"        Binding="{Binding Comment}" Width="160" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Length (mm)"    Binding="{Binding Length, StringFormat={}{0:F3}}" Width="95" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Width (mm)"     Binding="{Binding Width, StringFormat={}{0:F3}}" Width="95" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Thickness (mm)" Binding="{Binding Thickness, StringFormat={}{0:F3}}" Width="105" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Steps"          Binding="{Binding TotalStrokes, Mode=OneWay}" Width="60" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Created"        Binding="{Binding CreatedDate, StringFormat={}{0:dd-MMM-yyyy HH:mm}}" Width="140" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Updated"        Binding="{Binding ModifiedDate, StringFormat={}{0:dd-MMM-yyyy HH:mm}}" Width="140" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="8,6" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="Delete Old Data"
                        Command="{Binding DeleteOldDataCommand}"
                        Style="{StaticResource DangerButtonStyle}"/>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>
